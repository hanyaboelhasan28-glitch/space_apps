import 'package:flutter/material.dart';
class PlanetHeaderWidget extends StatelessWidget {
  const PlanetHeaderWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return Column(
      children: [
        PlanetHeaderWidget(

        )
      ],
    );
  }
}
